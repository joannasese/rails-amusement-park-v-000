<%= form_for @user, url: url_for(controller: 'sessions', action: 'create') do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %>
  <p>
  <%= f.label :password %><br>
  <%= f.text_field :password %>
  <p>
  <%= f.submit "Sign In"%>
<% end %>
